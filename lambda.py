# def double(x):
#     return x*2

# we can write same function by only using lambda function how let see 

double=lambda x:x*2 #same thing

# we can also write the function by using two arguments
avg = lambda x,y :(x+y)/2



print(double(2))
print(avg(4,2))